4 // Se agrega la referencia del algoritmo de firma utilizado. reference.DigestMethod = "http://www.w3.org/2001/04/xmlenc#sha256"; reference.Uri = ""; // Se agrega la información del certificado utilizado para la firma. signedXml.KeyInfo = keyInfo; // Generate the signature. //Obtenemos la representación del XML firmado y la guardamos en un XmlElement object //Adicionamos el elemento de la firma al documento XML. xmlFirmaDigital, true return xmlDoc; } catch { throw ex; } } ''' <summary> ''' </summary> ''' <param name="xmlDoc"> documento a firmar</param> ''' <param name="pathCert">ubicacion del certificado a utilizar para firmado</param> ''' <param name="passCert">contraseña del certificado digital</param> ''' <returns>XML firmado digitalmente</returns> Private Function Signed ByVal xmlDoc As XmlDocument, ByVal pathCert As String , ByVal passCert As String XmlDocument Try If Not Then Throw New "El certificado para firma no existe" Dim cert = New Dim True Dim key New 24 key .PersistKeyInCsp = False key Dim signedXml As SignedXml = New Método de firmado en VB.Net