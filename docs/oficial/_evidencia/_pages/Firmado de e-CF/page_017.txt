} } } if Array 0 // Eliminar espacios de nombres que ya están presentes en nsListToRender for var p1 in if continue ; var alreadyListed = false ; for var p2 in if nsListToRender[p2].prefix === ancestorNamespaces[p1].prefix { alreadyListed = true ; } } if } } } //renderizar espacios de nombres for a in if continue ; } p = nsListToRender[a]; " xmlns:" , p.prefix, '="' , p.namespaceURI, '"' } return { "rendered" "" "newDefaultNs" : newDefaultNs}; }; // Obtiene  el texto de caracteres encodeados private static var xml_special_to_encoded_attribute = { ' &' : '&amp;' , '<' : '&lt;' , '"' : '&quot;' , '\r' : '', '\n' : '' , '\t' : '&#x9; ', "'" : '&apos;' , '>' : '&gt;' 12