isBeforeDocument = null , isAfterDocument = null ; if var nextNode = node, previousNode = node; while nextNode !== null if isBeforeDocument = true ; break ; } nextNode = nextNode.nextSibling; } while previousNode !== null if isAfterDocument = true ; break ; } previousNode = previousNode.previousSibling; } } isAfterDocument ? "\n" : "" "<!--" + this "-->" isBeforeDocument ? "\n" : " }; private renderAttrs = function node, defaultNS var a, i, attr , res = [] , attrListToRender = []; if node.nodeType=== 8 return this if i = 0 attr = node.attributes[i]; // Para ignorar los atributos de definición del espacio de nombres. if "xmlns" 0 continue ; } } } for a in if continue ; } 10