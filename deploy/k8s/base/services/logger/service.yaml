apiVersion: v1
kind: Service
metadata:
  name: logger-service
  labels:
    app.kubernetes.io/name: logger-service
    app.kubernetes.io/part-of: getupnet
spec:
  selector:
    app.kubernetes.io/name: logger-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
