apiVersion: v1
kind: Service
metadata:
  name: admin-service
  labels:
    app.kubernetes.io/name: admin-service
    app.kubernetes.io/part-of: getupnet
spec:
  selector:
    app.kubernetes.io/name: admin-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
