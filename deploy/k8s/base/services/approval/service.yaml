apiVersion: v1
kind: Service
metadata:
  name: approval-service
  labels:
    app.kubernetes.io/name: approval-service
    app.kubernetes.io/part-of: getupnet
spec:
  selector:
    app.kubernetes.io/name: approval-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
